{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Backup to Google Drive",
      "detail": "Stage all, commit, and push — triggers the backup workflow automatically",
      "type": "shell",
      "command": "git add -A; git commit -m '${input:commitMessage}'; git push",
      "group": "build",
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      }
    },
    {
      "label": "Restore from Google Drive",
      "detail": "Triggers pull-from-gdrive workflow — run git pull manually once workflow completes",
      "type": "shell",
      "command": "gh workflow run pull-from-gdrive.yml --ref main --field confirm=YES",
      "group": "build",
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      }
    }
  ],
  "inputs": [
    {
      "id": "commitMessage",
      "type": "promptString",
      "description": "Commit message",
      "default": "update"
    }
  ]
}